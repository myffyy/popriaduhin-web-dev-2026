# Лабораторная работа № В-2

## Назначение

Проект реализует веб-калькулятор на PHP для вычисления арифметических выражений с целыми числами и десятичными дробями.

Поддерживаются:
- сложение `+`
- вычитание `-`
- умножение `*`
- деление `/` и `:`
- скобки `(` и `)`
- история вычислений через сессию PHP

## Структура проекта

- [`index.php`](/Users/myffy/Documents/Uni/2_курс/Веб(приложения)/ЛР_10/index.php) - основной файл приложения
- [`style.css`](/Users/myffy/Documents/Uni/2_курс/Веб(приложения)/ЛР_10/style.css) - стили страницы

## Как работает программа

1. При открытии страницы запускается сессия и инициализируется массив истории.
2. На странице отображается POST-форма с полем `val` и кнопкой `Вычислить`.
3. Если форма отправлена, выражение вычисляется функцией `calculateSq()`.
4. Функция `calculateSq()`:
   - проверяет корректность скобок через `SqValidator()`
   - находит внутренние выражения в скобках
   - вычисляет их рекурсивно
   - подставляет результат обратно в выражение
   - передает выражение без скобок в `calculate()`
5. Функция `calculate()` выполняет вычисление выражения без скобок с учетом приоритета операций.
6. Результат или сообщение об ошибке выводится перед формой.
7. История вычислений отображается в подвале и хранится в `$_SESSION['history']`.
8. Чтобы не дублировать запись при обновлении страницы, используется скрытое поле `iteration`.

## Реализованные функции

- `isnum($x)` - проверяет, является ли строка числом
- `calculate($val)` - вычисляет выражение без скобок
- `SqValidator($val)` - проверяет правильность расстановки скобок
- `calculateSq($val)` - вычисляет выражение со скобками

## Особенности реализации

- Не используется `eval()` для вычисления выражений.
- История вычислений сохраняется между загрузками страницы в рамках одной сессии.
- Текущий результат не попадает в историю сразу, а добавляется только при следующей загрузке страницы.
- Ввод ошибок обрабатывается сообщением вида `Ошибка вычисления выражения: ...`.

## Проверка

Перед сдачей проверялось:
- отсутствие синтаксических ошибок в `index.php`
- вычисление выражений с приоритетом операций
- корректная обработка скобок
- сохранение истории через сессию

## Ответы на контрольные вопросы

1. Вопрос: Как изменится работа функции из листинга В-2.4, если из нее убрать проверку на пустую строку?

   Ответ: Если убрать проверку на пустую строку, функция начнет пытаться обрабатывать пустое выражение как корректное. Это приведет к неверному разбору строки и ошибочным результатам.

2. Вопрос: Что такое рекурсия в программировании? Приведите примеры рекурсии.

   Ответ: Рекурсия - это прием, при котором функция вызывает саму себя. Примеры: вычисление выражений со скобками, факториал числа, обход дерева.

3. Вопрос: Что такое база рекурсии?

   Ответ: База рекурсии - это условие, при котором рекурсивная функция завершает работу без дальнейших самовызовов.

4. Вопрос: Почему математические действия в функции `calculate()` выполняются именно в такой последовательности?

   Ответ: Потому что в математике операции имеют разный приоритет: сначала вычисляются скобки, затем умножение и деление, после этого сложение и вычитание.

5. Вопрос: Что такое сессия в РНР?

   Ответ: Сессия в PHP - это механизм хранения данных пользователя между запросами в рамках одного сеанса работы браузера.

6. Вопрос: Почему нельзя начинать инициализировать данные сессии после любого вывода?

   Ответ: Потому что `session_start()` работает через HTTP-заголовки, а заголовки должны быть отправлены до любого вывода HTML или текста.

7. Вопрос: Можно ли инициализировать данные сессии внутри тега `<body>`, но при до вывода данных средствами РНР?

   Ответ: Формально данные можно инициализировать до вывода, но делать это внутри `body` небезопасно и неправильно, если до этого уже был какой-либо вывод. `session_start()` нужно вызывать в самом начале документа.

8. Вопрос: Как РНР разделяет пользователей для доступа к разным хранилищам сессий?

   Ответ: PHP использует уникальный идентификатор сессии. Он хранится в cookie или передается через URL, если cookies недоступны.

9. Вопрос: Как получить доступ к данным сессии из РНР?

   Ответ: Через суперглобальный массив `$_SESSION`.

10. Вопрос: Как долго можно обращаться к данным сессии?

    Ответ: Пока сессия активна. Обычно это длится до закрытия браузера или до истечения времени жизни сессии на сервере.

11. Вопрос: Можно ли хранить в сессии массивы и строки больше 1024 символов?

    Ответ: Да, можно, если это допускается конфигурацией сервера и размером хранилища сессий.

12. Вопрос: Как записать данные в сессию?

    Ответ: Присваиванием значения элементу массива `$_SESSION`, например: `$_SESSION['key'] = $value;`.

13. Вопрос: Можно ли прочитать данные из сессии если они туда не записаны? Как быть в том случае, если такая ситуация возможна?

    Ответ: Напрямую читать нельзя без проверки. Нужно сначала проверять существование элемента через `isset()`, чтобы избежать предупреждений.

14. Вопрос: Как быстро вычислить любое выражение средствами РНР?

    Ответ: Самый быстрый способ - использовать `eval()`, но в этой лабораторной работе его применять нельзя.

15. Вопрос: Можно ли в РНР интерпретировать символ строки как число?

    Ответ: Да, PHP может интерпретировать строковый символ как число в арифметических операциях или при явном преобразовании типов, но для корректной проверки ввода это нужно контролировать отдельно.
